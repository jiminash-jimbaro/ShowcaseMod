{
  "cmake.configureEnvironment": {
    "GEODE_SDK": "${workspaceFolder}/../../geode"
  },
  "cmake.buildDirectory": "${workspaceFolder}/build",
  "terminal.integrated.env.windows": {
    "GEODE_SDK": "${workspaceFolder}/../../geode"
  }
}
